# Applyze

An AI-powered resume analyzer and job application tracker (MERN): maintain one master profile, generate tailored PDF resumes from it, track job applications individually, and get an AI-generated fit/ATS report per application — grounded in your real experience via retrieval (RAG), so the AI can't invent qualifications.

Full architecture and phased build plan: see `frontend`/`backend` sections below, and the original plan doc for schema/API/LangChain design details.

## Project structure

```text
react-ai-resume-analyzer/
├── frontend/   # React Router v8 (SSR), Tailwind CSS v4, TypeScript
└── backend/    # Express + TypeScript, MongoDB (Mongoose), LangChain.js
```

## Status

- [x] **Phase 0 — Repo restructure.** Moved the original React Router app into `/frontend`, created `/backend` as a sibling directory, updated `.gitignore` for both workspaces.
- [x] **Phase 1 — Backend scaffold + real JWT auth.** Complete.
  - [x] `backend/` initialized with TypeScript, Express 5, Mongoose, bcrypt, jsonwebtoken, cookie-parser, cors, dotenv, zod.
  - [x] `tsconfig.json` (strict mode, NodeNext, explicit `"types": ["node"]` — required as of TypeScript 7) and `dev`/`build`/`start` npm scripts.
  - [x] Environment config validation (`src/config/env.ts`) — Zod-validated `.env`, fails fast at boot if anything required is missing.
  - [x] MongoDB Atlas connection (`src/config/db.ts`).
  - [x] Express app + entry point (`src/app.ts`, `src/server.ts`) — `GET /api/health` verified working end-to-end (env → DB → server).
  - [x] `User` model (hashed password, hashed/revocable refresh tokens per session).
  - [x] Auth backend complete: `POST /api/auth/signup|login|refresh|logout`, `GET /api/auth/me`. Full cycle verified via curl — signup → protected `/me` → token rotation via `/refresh` → `/logout` → confirmed logged out.
  - [x] Frontend: `login.tsx`, `signup.tsx`, `api.server.ts`/`session.server.ts` (SSR cookie forwarding + `requireUser`). Home route protected and verified in-browser end-to-end.
- [x] **Phase 2 — Subscription billing (Stripe).** Complete. Free tier (3 AI analyses/month) + paid tier ($9/mo or ~$79/yr), Stripe Checkout (hosted) + Billing Portal, embedded on `User` model.
  - [x] Steps 1-2: Stripe Dashboard Product/Prices created; `stripe` SDK installed; `env.ts`/`.env.example` extended with `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`STRIPE_PRICE_ID_MONTHLY`/`STRIPE_PRICE_ID_YEARLY`; `User.model.ts` extended with `subscription`/`usage` sub-schemas.
  - [x] Step 3: `stripe.service.ts`, `billing.controllers.ts` (`checkout`, `portal`), `billing.routes.ts`, mounted in `app.ts`. Verified via curl — `POST /api/billing/checkout` returns a real Stripe Checkout URL; `POST /api/billing/portal` correctly 400s before a Stripe customer exists.
  - [x] Step 4: Frontend `pricing.tsx` (two `Form`s → `POST /api/billing/checkout` → `redirect()` to Stripe). Verified in-browser — both plan buttons land on the correct hosted Checkout page.
  - [x] Step 5: Real test-mode checkout completed with `4242 4242 4242 4242`; redirected to `/billing?checkout=success`, 404 as expected (`billing.tsx` doesn't exist until step 7).
  - [x] Step 6: Webhook wiring — `app.ts` restructured so `/api/billing/webhook` gets `express.raw()` ahead of the global `express.json()`; `handleStripeWebhook` handles `checkout.session.completed`/`customer.subscription.updated`/`customer.subscription.deleted`. Verified end-to-end with `stripe listen` — real test checkout flips `subscription.status` to `"active"` in MongoDB with correct customer/subscription/price IDs.
  - [x] Step 7: Frontend `billing.tsx` (button → `POST /api/billing/portal` → `redirect()` to Stripe's hosted Billing Portal). Verified in-browser — cancelling a subscription there correctly leaves `status: "active"` with `cancelAtPeriodEnd: true` until period end.
  - [x] Step 8: `usage.service.ts` (`resetUsageIfNeeded`, `isUnderLimit`, `incrementUsage`) + `requireActiveSubscription.middleware.ts` — built but not mounted on any route yet (no analysis endpoint exists until Phase 6). Verified via a throwaway REPL script: 3 free-tier analyses allowed, 4th blocked (402-worthy), backdated `resetAt` correctly zeroes the counter and rolls to next month.
  - [x] Step 9: Extended `GET /api/auth/me` to include `subscription`/`usage` (running `resetUsageIfNeeded` on every call) — additive change, verified via curl that a fresh user gets correct free-tier defaults and existing `id`/`email`/`name` fields are unaffected.
  - **Bugs caught and fixed during testing**: this Stripe SDK version (22.4.0, API `2026-07-29`) moved `current_period_end` from `Subscription` down to each `SubscriptionItem` (`subscription.items.data[0].current_period_end`), and the Billing Portal's "cancel at period end" flow sets a `cancel_at` timestamp rather than the older `cancel_at_period_end: true` boolean — `cancelAtPeriodEnd` is now derived from both signals (`cancel_at_period_end || cancel_at !== null`) to handle either case correctly.
- [x] **UI pass — landing page, routing restructure, dashboard redesign.** `/` is now a public marketing landing page (`routes/landing.tsx`) describing the app's functionality and pricing, with login/signup as a modal (nested routes + `Outlet`); the authenticated app home moved from `home.tsx` at `/` to `routes/dashboard.tsx` at `/dashboard`. Login/signup redirect to `/dashboard`; already-logged-in visitors to `/` are redirected there too. Added a working logout button (`routes/logout.tsx`, resource route) and a red-tinted `.logout-button` style to set it apart from other nav actions. Added `MobileNavbar.tsx` (hamburger + animated dropdown) and `lucide-react` icons throughout. Dashboard now groups applications into 4 status categories (Live/Rejected/Built & Sent/Skipped) behind a tab filter, plus a "Start New Analysis" CTA (routes to the not-yet-built `/applications/new`). Added `routes/insights.tsx` ("Recurring Patterns Worth Knowing," later renamed from `patterns.tsx` to Insights) as a letter/report-style page — **currently mock data only** (`app/constants/index.ts`), to be replaced by a real AI-generated feature in Phase 8 below. Also fixed a real `.auth-button` overflow bug and a CSS cascade-layers bug (bare global selectors were unlayered and always beat layered component classes regardless of specificity).
- [x] **Phase 3 — Profile CRUD, manual-entry half.** `Profile` model, `GET/PUT /api/profile`, `profileFormStore` (zustand draft state), full `profile.tsx` form (contact info, summary, skills, work history, projects, education, certifications, all as repeatable sections). Verified: profile persists and reloads correctly.
- [x] **Phase 3 — PDF-upload autofill, remaining half.** Upload an existing resume PDF and have the profile form pre-filled via AI-structured extraction — the one deliberate AI exception in an otherwise AI-free phase. Backend: `ai/schemas/profileParse.schema.ts` (lenient/optional Zod shape, since resume text is messy — strict validation still happens later via the existing `profileUpdateSchema` on save), `ai/chains/parseResume.chain.ts` (`ChatAnthropic.withStructuredOutput`, anti-hallucination system prompt), `POST /api/profile/parse-resume` (returns structured JSON, never auto-saved, `502` on upstream Claude failure). Frontend: `lib/extractResumeText.client.ts` (`pdfjs-dist`, client-side only via the `.client.ts` suffix), a same-origin resource route `routes/profile.parse-resume.ts` (avoids exposing the backend URL publicly / CORS — browser → frontend SSR → backend, cookies forwarded via the existing `apiFetch`), upload UI on `profile.tsx` pre-filling `profileFormStore` via the existing `mapProfileToDraft` (schema shapes matched on purpose, no new mapping code needed). Verified end-to-end in-browser: real resume PDF → accurate pre-fill → saved.
  - **Bug caught during testing**: Claude 5-generation models reject the `temperature` param entirely (`` `temperature` is deprecated for this model ``) — removed from the `ChatAnthropic` constructor.
- [x] **Phase 3 — extras beyond original scope.** Cloudinary-backed profile photo upload (`services/cloudinary.service.ts`, signed uploads — the browser uploads directly to Cloudinary using a short-lived signature from the backend, image bytes never touch our server, API secret never reaches the client), auto-saved immediately on upload rather than requiring a separate "Save profile" click. Card-based redesign of `profile.tsx` (one tinted card per section, responsive grid, avatar + Save button moved into a compact header). App-wide toast notification system (`stores/toastStore.ts` + `components/Toaster.tsx`, mounted globally in `root.tsx`) replacing an easy-to-miss inline "saved" banner. Simulated progress-bar modal during AI resume parsing. Global `Footer.tsx`, also mounted in `root.tsx`, shown on every route regardless of auth state.
- [x] **Phase 4 — Account & security hardening.** Account deletion, email verification via Resend, password recovery, and opt-in phone/SMS MFA via Twilio Verify; see plan doc's "Account & security hardening" section. Note: plan cancel/change was *not* new work — it already worked via Phase 2's Stripe Billing Portal.
  - [x] `settings.tsx` shipped — sidebar (Account/Billing/Security/Danger zone) + tinted content panels, deliberately distinct from the rest of the app's card-grid pages.
  - [x] Standalone `/billing` page removed and folded into Settings' Billing tab — real in-app plan switching, cancel/reactivate, and payment-method management (new `/api/billing/cancel|reactivate|change-plan` endpoints), not just a link out to Stripe's hosted portal.
  - [x] Account deletion — `DELETE /api/auth/me` (password re-confirm, cancels any active Stripe subscription first, deletes Profile + User, clears cookies) with a type-to-confirm modal in Settings' Danger zone. Verified end-to-end via curl (wrong password rejected, correct password deletes and logs out, re-login fails).
  - [x] Email verification — Resend-backed (`services/email.service.ts`), hashed 24h token on `User.emailVerificationToken` (same pattern as refresh tokens). `signup` sends the email (non-blocking), `POST /api/auth/verify-email` confirms it, `POST /api/auth/resend-verification` (60s cooldown, authenticated) resends. Frontend: `routes/verify-email.tsx` (reads `?token=`, shows a success/failure card), Settings' Security tab shows real status + resend button. Verified end-to-end with a real inbox: signup email received, link click flips `emailVerified` to `true` in MongoDB, invalid tokens and cooldown both rejected correctly.
  - [x] Password recovery — reuses the email-verification infra: `User.passwordResetToken` (same hashed-token shape, shorter 1h expiry), `sendPasswordResetEmail` in `email.service.ts`. `POST /api/auth/forgot-password` always returns the same generic response regardless of whether the email exists (no account enumeration); `POST /api/auth/reset-password` validates the token, updates `passwordHash`, and clears `refreshTokens: []` so a reset kills every existing logged-in session, not just future ones. Frontend: `routes/forgot-password.tsx` (email → generic confirmation) and `routes/reset-password.tsx` (reads `?token=`, new-password + confirm form), with a "Forgot password?" link on `login.tsx`. Verified end-to-end against the real send flow (grabbed the real token from the actually-delivered email via the Resend API): old password rejected after reset, new password works, the pre-reset session's refresh token is revoked, and a reused/invalid token is rejected.
  - [x] Opt-in phone/SMS MFA — Twilio Verify (`services/mfa.service.ts`, wraps `verifications.create`/`verificationChecks.create`; Verify manages the sending number internally, no Twilio phone number needed). `User.mfa: {phoneNumber?, phoneVerified, enabled}`. Setup: `POST /api/auth/mfa/phone/start` (texts a code; refuses to run if MFA is already enabled, preventing a lockout from overwriting a live number with an unverified one), `POST /api/auth/mfa/phone/confirm`, `POST /api/auth/mfa/enable|disable`. Login: when `mfa.enabled`, `login` withholds real cookies and instead texts a code and returns a short-lived MFA challenge JWT (~5 min, `{userId, purpose: "mfa_challenge"}`, deliberately signed with `JWT_REFRESH_SECRET` rather than `JWT_ACCESS_SECRET` so it can never be replayed as a real access token against `requireAuth`); `POST /api/auth/mfa/verify-login` completes the session. Frontend: Settings' Security tab gets an iOS-style toggle plus a compact phone-number row (country-code dropdown defaulting to +1, custom chevron since native `<select>` arrows aren't reliably styleable via padding); `login.tsx` gets a second "enter your code" screen keyed off the same action via a `challengeToken` field. Bug caught during testing: Twilio's `verificationChecks.create` throws a 404 (not a "denied" status) once a code has already been approved/consumed — `checkVerificationCode` now catches that specifically and returns `false` instead of letting an unhandled exception leak Express's default HTML stack trace to the client. Verified end-to-end with a real phone number: normal login unaffected before MFA is enabled; after enabling, password alone no longer grants access, a challenge token used directly as an access-token cookie is correctly rejected, the right code completes login and sets real cookies, and a reused/stale code is cleanly rejected.
- [x] **UI pass — landing page redesign, site-wide Contact form.** `routes/landing.tsx` rebuilt as a real marketing page (how-it-works steps, feature grid, and a genuine 3-tier pricing section — Free / Pro Monthly / Pro Yearly, the yearly card marked "Best value") instead of a bare feature list. `components/ContactForm.tsx` extracted as a shared component (used on both `routes/contact.tsx` and embedded directly in a two-column section on the landing page, so anonymous visitors don't have to leave the page to ask a question) and fully wired: `POST /api/contact` validates with Zod, saves a durable record to a new `ContactMessage` Mongo collection, and emails a notification to `CONTACT_RECEIVER_EMAIL` via Resend (`reply-to` set to the submitter's address so replying goes straight back to them; the DB save happens regardless of whether the email send succeeds). `Navbar`/`MobileNavbar`/`Footer` are now consistently auth-aware (`root.tsx` gained a loader so `Footer` can read auth state via `useRouteLoaderData`) — Contact links and account-only links only show once logged in. Verified end-to-end: submission saves to Mongo, notification email confirmed `delivered` via the Resend API with the correct `reply-to`.
- [x] **Phase 5 — PDF generation from profile.** `@react-pdf/renderer` used declaratively (its own `<Document>/<Page>/<View>/<Text>` primitives, not HTML/CSS) to render a resume server-side. `backend/src/pdf/templates/ClassicResumeTemplate.tsx` + `renderResumePdf.tsx` (`renderToBuffer` wrapper), `POST /api/profile/preview-pdf`. Required adding `react`/`@types/react` as explicit backend deps and `"jsx": "react-jsx"` in `tsconfig.json` (react-pdf reuses React's component model to describe PDF output, not the DOM, but still needs real JSX support). Refactored `Profile.model.ts` to export a shared `ResumeContent` interface + sub-schemas (contactInfo/workHistory/project/education/certification) separate from `ProfileDocument`, so the same template can render both a real profile and, later, an AI-tailored `Document` snapshot without duplicating the shape. Verified via curl against real profile data plus actual visual review of the generated PDF, not just a 200 status. **Bug caught during testing**: `@react-pdf/renderer` auto-paginates, and a `flexDirection: 'row'` bullet row (dot + text) landing right at a page boundary split across pages, producing an orphaned bullet — fixed with `wrap={false}` on that row so a whole bullet moves to the next page instead of tearing. Frontend "Preview PDF" button deliberately not built — superseded by Phase 6's "download tailored resume" UI.
- [x] **Phase 6 — Application tracker + AI fit-scoring + AI-tailored resume generation.** *(Merged from the plan's original separate Phase 6/Phase 7 split — see the plan doc's 2026-08-07 update. The real ask was JD-in → score + tailored-resume-out in one flow, not a two-step no-AI-then-AI rollout.)*
  - Models: `Application` (status enum, `jobDescriptionText`, `currentDocumentId`/`currentFeedbackReportId`), `Document` (versioned resume snapshot, `contentSnapshot: ResumeContent` reusing Profile's shared sub-schemas), `FeedbackReport` (fixes the long-standing `ATS.tips[].explanation` inconsistency — every category's tips now share one `tipSchema`).
  - CRUD: `GET/POST /api/applications`, `GET/PATCH/DELETE /api/applications/:id` (ownership-scoped via a combined `{_id, userId}` query filter; delete cascades to any `Document`/`FeedbackReport` rows so nothing orphans). Verified the full loop via curl — create → list → get → patch status → delete → confirmed 404 after.
  - AI: `ai/schemas/feedback.schema.ts` + `ai/schemas/resumeContent.schema.ts` (deliberately scoped — the AI only tailors `summary`/`workHistory` bullets/`projects`/`skills`; `contactInfo`/`education`/`certifications` are copied straight from the real profile and never touched by the model, shrinking the hallucination surface), run as two concurrent `ChatAnthropic.withStructuredOutput()` calls in `analyzeApplication.chain.ts` (originally one combined call — see the "resume template restyle" entry below for why that changed). `ai/prompts/resumeFeedback.prompt.ts` formats the whole profile as plain text (no RAG yet — Phase 9). `POST /api/applications/:id/analyze` persists both a `FeedbackReport` and a tailored `Document`, and is the real hook point for the Phase 2 `requireActiveSubscription` usage-limit middleware, finally wired in here.
  - PDF download: `GET /api/documents/:id/download` (backend) + a same-origin resource route `routes/documents.$id.download.ts` (frontend, same cross-origin-avoidance pattern as Phase 3's `parse-resume` route) so the browser downloads through the frontend origin.
  - Frontend: `dashboard.tsx` loader replaced with real `GET /api/applications` data (plus a parallel per-application feedback fetch for card scores — a small accepted N+1 for a personal-scale app rather than premature optimization); `ResumeCard.tsx` retyped from the old mock `Resume` to real `Application`, with a generic placeholder thumbnail (real PDF thumbnails stay deferred to Phase 9 per the plan). New `applications.new.tsx` (company/title/JD form) and `applications.$id.tsx` (job description, score breakdown with good/improve tips, download button, and a `useFetcher`-driven "Run/Re-run analysis" action shared with the initial-failure retry path). A shared `AnalyzingModal.tsx` (non-dismissible, `beforeunload`-guarded) covers both the initial submit and the fetcher-driven retry, with honest copy — refreshing doesn't corrupt anything server-side, it just risks a wasted duplicate analysis (and a second counted usage credit) if you resubmit.
  - **Bugs caught during testing**: (1) the combined AI call needs an explicit `maxTokens: 8192` on the `ChatAnthropic` constructor — the library's default limit was silently truncating the large combined output into an unparseable empty `{}`. (2) Even with that fixed, Claude occasionally double-encodes a nested object as a JSON string within the same tool call (non-deterministic, more likely on larger real profiles) — mitigated with a bounded 3-attempt retry loop around the structured-output call in `analyzeAndTailorResume`, safe to retry since usage only increments after a call actually succeeds. (3) A stray hyphen artifact in the PDF's Skills line from `@react-pdf/renderer`'s internal line-breaking engine — fixed by switching the skills separator from bullet-dots to commas, sidestepping the whole class of bug rather than fighting the renderer's hyphenation internals.
  - **Unrelated but real bug found and fixed via normal usage of this phase's UI**: `app/lib/session.server.ts` never actually called `POST /api/auth/refresh` on an expired access token, so any 15-minute gap between requests silently bounced users to `/login` even though their 30-day refresh-token cookie was still perfectly valid. Fixed with a "silent refresh via self-redirect" — on a 401, `getUser` now refreshes and `redirect()`s back to the same URL carrying the newly-rotated `Set-Cookie` headers, so the browser retries transparently with no visible login screen. Caught a React Router v8-specific gotcha while building this: client-side `<Link>` navigation fetches loader data from a `/route.data` URL (the "single fetch" convention), so the redirect target must strip a trailing `.data` or it redirects straight to that internal data endpoint instead of the real page. Verified for real: deleted just the access-token cookie in devtools, clicked a link, confirmed silent recovery.
  - Verified fully end-to-end against real infrastructure: a real JD pasted → a real Claude call → a real score plus a real tailored PDF downloaded and visually reviewed (not just status-code checked), across three different real applications — including the one that hit and recovered from the retry bug above.
- [x] **UI/data pass — resume template restyle to match a real reference resume.** User supplied a real PDF resume as the target layout; restyled `ClassicResumeTemplate.tsx` to match it (pipe-separated header led by location, per-role location line, education as a two-line degree/institution layout, inline project tech-stack parentheticals, comma-bulleted skills) and made two real schema additions to get there: `WorkHistoryEntry.location`/`EducationEntry.location` (plain facts, never touched by the AI — same treatment as `contactInfo`), and skills changed from a flat `string[]` to categorized `{category, items[]}[]` matching the reference's grouped layout. Touched `Profile.model.ts`, `Document.model.ts`, the profile-save Zod schema, `profileParse.schema.ts` (AI resume-upload extraction), `resumeContent.schema.ts` (AI tailoring), the prompt's plain-text profile dump, and the profile form UI (categorized skill-group inputs, location fields).
  - **Migration risk caught before it caused real data loss**: the account's existing saved profile had the old flat `skills: string[]` shape; reading it against the new subdocument schema made Mongoose silently drop every skill (cast failure → empty array), which would have been written back as permanently empty on the next save. Caught by inspecting the raw document via a temp script *before* assuming the new code worked, not after. Fixed by re-importing the same reference resume PDF through the existing AI-extraction upload flow rather than hand-migrating data — doubled as a real test of the updated extraction schema.
  - **AI reliability regression found and fixed**: the categorized-skills schema made the existing combined `{feedback, tailoredResume}` structured-output call noticeably less reliable — Claude would occasionally double-encode one half as a JSON *string* instead of a real nested object (3/3 consecutive failures in testing at the new schema depth; a higher `maxTokens` didn't help, confirming it wasn't a truncation issue). Fixed by splitting back into two separate, shallower structured-output calls run concurrently via `Promise.all` — each schema alone is reliable, and running them concurrently avoids doubling wall-clock latency.
  - **Location-leak bug caught via full visual PDF review, not just a status check**: adding `job.location` to the AI's plain-text prompt input (with nowhere in its output schema to put it back, since location is deliberately AI-untouched) taught the model to fold it into the `company` field instead (e.g. `"Freelancer, Remote"`), which also silently broke the controller's company-name lookup used to reattach the real location afterward. Fixed by removing location from the AI's prompt input entirely — it doesn't affect scoring or tailoring, and the controller already reattaches the real value from the profile regardless.
  - **Auth-retry gap closed across the app, not just the one route that surfaced it**: the bug that prompted this was "Import from resume" failing with a generic, unhelpful error — turned out to be the same stale-access-token issue fixed for page navigation in Phase 6, but for a resource-route action, which never goes through `session.server.ts`'s loader-time silent refresh. Added a reusable `apiFetchWithAuthRetry()` in `api.server.ts` (retries once via a silent refresh on a 401, relaying any rotated `Set-Cookie` headers) and applied it to all three profile resource routes plus both `applications.new.tsx`/`applications.$id.tsx` actions, since they have the same shape (long-running or SPA-triggered actions with no loader in between). The resume-parse error is now also surfaced as a toast, not just inline text.
  - `AnalyzingModal.tsx` generalized to accept custom `title`/`description`/`warning` copy and reused for resume parsing on `profile.tsx`, replacing a fake `setInterval`-driven progress bar that didn't track anything real.
  - Bullet glyph reverted from `●` to `•` after a real rendering bug: the built-in PDF `Helvetica` font's WinAnsi character set doesn't include `●` (U+25CF), so it silently rendered as a garbled, wrong glyph — `•` (U+2022) is in that set and was already proven to work.
  - **Tailoring depth fix**: real testing (not assumption) showed the AI was under-tailoring — bullets came back nearly word-for-word identical to the profile despite the schema saying "reworded." Root cause was weak/ambiguous wording plus a real gap (`projects[].bullets` had no rewording instruction at all, unlike `workHistory[].bullets`). Fixed by making `resumeContent.schema.ts`'s field descriptions and the prompt's RULES section explicitly forbid near-verbatim copying, require restructured sentences and JD terminology substitution while keeping every fact identical, and adding a concrete before/after example anchoring what "under-tailored" looks like. Verified by diffing tailored bullets against the source profile line-by-line — every bullet now shows real rewording with zero fabricated facts.
- [ ] **Phase 7 — AI chatbot assistant** (new — read-only Q&A grounded in the user's own profile/applications/feedback reports, no write/tool-calling access; `ChatAnthropic` with streaming, see plan doc's "AI chatbot assistant" section)
- [ ] **Phase 8 — Cross-application insights (Insights page)** (aggregate `FeedbackReport`s in code, one `ChatAnthropic.withStructuredOutput` call turns that into recurring patterns + technologies to study + a learning roadmap — replaces today's mock data on `/insights`; no LangGraph needed, this isn't an agentic/multi-step workflow, see plan doc's "Cross-application insights (Patterns page)" section)
- [ ] **Phase 9 — Embeddings + RAG grounding** (OpenAI embeddings + MongoDB Atlas Vector Search)
- [ ] **Phase 10 — Stretch/polish** (status UX, resume version history, cover letters, thumbnails, rate limiting, tests)

## Key architecture decisions

- **Auth**: real JWT (access + DB-backed revocable refresh tokens) + bcrypt, from v1.
- **LLM**: Anthropic (Claude) via LangChain.js for all reasoning/writing.
- **Embeddings**: OpenAI `text-embedding-3-small` (Claude has no embeddings API) — used only for the RAG vector store, never for writing.
- **Vector store**: MongoDB Atlas Vector Search — no separate vector DB service. Requires a real Atlas cluster (not local MongoDB) from Phase 1 onward.
- **PDF generation**: `@react-pdf/renderer`, rendered server-side in `backend/`.
- **Client state**: Zustand for ephemeral UI/form state only (auth cache, form drafts, multi-step flows) — persisted data always lives in MongoDB via the API.

## Frontend

React Router v8 (SSR via `@react-router/serve`), React 19, Tailwind CSS v4, TypeScript.

```bash
cd frontend
npm install
npm run dev        # http://localhost:5173
npm run build
npm run typecheck
```

## Backend

Express + TypeScript, MongoDB via Mongoose, LangChain.js (Anthropic + OpenAI embeddings).

```bash
cd backend
npm install
npm run dev         # tsx watch, http://localhost:5000
npm run build        # tsc -> dist/
npm run start        # node dist/server.js
```

Requires a `.env` file with: `PORT`, `NODE_ENV`, `MONGODB_URI`, `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `FRONTEND_URL`, plus (from Phase 2 onward) `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID_MONTHLY`, `STRIPE_PRICE_ID_YEARLY`; (from Phase 3 onward) `ANTHROPIC_API_KEY`, `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`; (from Phase 4 onward) `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_VERIFY_SERVICE_SID`; (from the Contact form onward) `CONTACT_RECEIVER_EMAIL`; and (from Phase 9 onward) `OPENAI_API_KEY`. See `backend/src/config/env.ts` for the authoritative Zod-validated list.

---

Built with React Router, Express, MongoDB, and LangChain.js.
